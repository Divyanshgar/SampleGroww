version: "2"
sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "database/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
